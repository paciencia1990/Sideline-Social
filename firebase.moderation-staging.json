{
  "functions": [
    {
      "source": "moderation-reporting-staging",
      "codebase": "moderation-reporting-staging",
      "runtime": "nodejs22",
      "predeploy": [
        "node \"$RESOURCE_DIR/scripts/assert-staging-project.cjs\"",
        "npm --prefix \"$RESOURCE_DIR\" run build",
        "npm --prefix \"$RESOURCE_DIR\" run verify:isolation",
        "npm --prefix \"$RESOURCE_DIR\" run verify:security-exception"
      ]
    }
  ]
}
