/**
 * Turn advancement is server-authoritative. The callable validates the host,
 * participant membership, current question, and allowed state transition.
 */
export { advanceTriviaGameSession } from "./gameState";
