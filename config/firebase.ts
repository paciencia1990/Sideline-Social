// Firebase auto-initializes from native config files:
// - android/app/google-services.json (Android)
// - ios/GoogleService-Info.plist (iOS)
//
// Do NOT call initializeApp() here — React Native Firebase handles initialization
// automatically when the native files are present.
//
// Import auth and firestore directly in your screens/services:
//   import auth from '@react-native-firebase/auth';
//   import firestore from '@react-native-firebase/firestore';

export {};